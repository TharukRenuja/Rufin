#!/usr/bin/env bash
set -euo pipefail

if [[ $# -lt 2 ]]; then
  echo "usage: $0 NIX_TARGET COMMAND [ARG...]" >&2
  exit 2
fi

nix_target="$1"
shift

if [[ $# -eq 4 && "$1" == rustup && "$2" == toolchain && "$3" == list && "$4" == --quiet ]]; then
  exit 0
fi

exec bash scripts/retry-nix nix --accept-flake-config develop "$nix_target" --command "$@"
