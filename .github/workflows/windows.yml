name: Windows

on:
  release:
    types:
      - published
  workflow_dispatch:
    inputs:
      ref:
        description: Git ref to build, including a test commit for an existing release
        required: true
        default: main
      upload_release_tag:
        description: Optional existing GitHub release tag to upload the installer to
        required: false
        default: ""

permissions:
  contents: read

concurrency:
  group: windows-release-${{ github.event_name == 'release' && github.event.release.tag_name || inputs.upload_release_tag || github.ref }}
  cancel-in-progress: true

jobs:
  build:
    name: Windows
    uses: ./.github/workflows/windows-build.yml
    with:
      ref: ${{ github.event_name == 'release' && github.event.release.tag_name || inputs.ref }}
      release_tag: ${{ github.event_name == 'release' && github.event.release.tag_name || inputs.upload_release_tag }}
      verify_signed_tag: ${{ github.event_name == 'release' }}

  upload:
    name: Upload Windows release asset
    if: github.event_name == 'release' || inputs.upload_release_tag != ''
    needs: build
    runs-on: ubuntu-latest
    permissions:
      contents: write
      id-token: write
      attestations: write
      artifact-metadata: write

    steps:
      - name: Download workflow artifact
        uses: actions/download-artifact@3e5f45b2cfb9172054b4087a40e8e0b5a5461e7c # v8
        with:
          name: Rufin-setup.exe
          path: dist

      - name: Attest Windows installer
        uses: actions/attest@f7c74d28b9d84cb8768d0b8ca14a4bac6ef463e6 # v4
        with:
          subject-path: dist/Rufin-*-setup.exe

      - name: Upload release asset
        env:
          GH_TOKEN: ${{ github.token }}
          RELEASE_TAG: ${{ github.event_name == 'release' && github.event.release.tag_name || inputs.upload_release_tag }}
        run: |
          set -euo pipefail
          installer="$(find dist -maxdepth 1 -name 'Rufin-*-setup.exe' -print -quit)"
          test -n "$installer"
          gh release upload "$RELEASE_TAG" "$installer" --clobber --repo "$GITHUB_REPOSITORY"
